export default function Logo({ className = '' }) {
    return (
      <span className={`font-bold text-lg tracking-tight text-slate-900 dark:text-gray-300 ${className}`}>
        MoringaPair
      </span>
    );
  }
